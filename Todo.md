Flow 

as soon as a file is opened the thing will fetch the indices of all the possible information 


User opens the fuzzy finder , there are two types so the either the user can provide the file type or it will infer automatically from the file extension

for the indices implement a regex based search with many go routines

after that display the best regex match in fuxxy finder
